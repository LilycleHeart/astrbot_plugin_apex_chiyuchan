# Apex API 请求（含 HTTP/2 支持）
httpx[http2]>=0.28.0

# Steam 日活卡片等 Jinja2 模板渲染
jinja2>=3.0.0

# SQLite 异步数据库
aiosqlite>=0.20.0

# Pillow 图片渲染（Playwright 不可用时的回退方案）
Pillow>=10.0.0

# HTML→PNG 卡片渲染
playwright>=1.48.0

# LLM 工具 CallToolResult 类型
mcp>=1.20.0,<2
